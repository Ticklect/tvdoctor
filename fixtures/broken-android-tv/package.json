{
  "name": "@tvdoctor/broken-android-tv",
  "version": "0.0.0",
  "private": true,
  "description": "Controlled native Android TV fixture with one seeded remote-focus defect.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-apk.ps1",
    "test": "node --test tests/*.test.mjs",
    "verify:apk": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-apk.ps1"
  }
}
